# Changelog

## [0.1.0] - 2025-03-06

### Added

- **ContextCraft: Generate Context Pack** — Run `contextcraft init` on the workspace.
- **ContextCraft: Update Context Pack** — Run `contextcraft update`.
- **ContextCraft: Diff Context Pack** — Run `contextcraft diff`.
- Output panel with dark/light theme support.
- Settings: `contextcraft.apiKey`, `contextcraft.model`, `contextcraft.maxTokens`, `contextcraft.pythonPath`.
